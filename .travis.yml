language: ruby
rvm:
  - 1.9.3-p484
before_install:
- git submodule update --init --recursive
- gem install rubocop
script: pwd
script: ls -l
script: rubocop
# script: bundle exec rspec
branches:
  only:
  - master
